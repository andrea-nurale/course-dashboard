export interface Customer {
    lastName: string;
    firstName: string;
    email: string
    id?: number
}

export  interface User {
    age: number
    lastName: string
    firstName: string
    gender: string
    id?: number

}